# 核心Web框架
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 数据验证和配置
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# 数据库
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9
pymongo==4.6.0
redis==5.0.1

# 任务队列
celery==5.3.4

# 认证和安全 (暂时保留，后续根据需要启用)
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4
# python-multipart==0.0.6

# AI API客户端 (暂时保留，后续根据需要启用)
# openai==1.3.7
# anthropic==0.7.8

# HTTP客户端 (暂时保留，后续根据需要启用)
# httpx==0.25.2

# 开发工具
black==23.11.0
isort==5.12.0
pytest==7.4.3
